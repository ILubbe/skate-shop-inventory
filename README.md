## Java Spring Skateboard Shop Inventory Management Project

#To run:

podman build -t java-spring-skate-shop .

podman run -d -p <host-port>:8080 --name java-spring-skate-shop java-spring-skate-shop
